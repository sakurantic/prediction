数据获取//
1.实时的二氧化硫（SO2）、二氧化氮（NO2）、一氧化碳（CO）、臭氧（O3）、可吸入颗粒物（PM10）、细颗粒物（PM2.5）
等六种环境空气污染物基本项目的浓度监测数据和空气质量指数（AQI） 
暂时先只分析pm2.5（别的未拿到）
气象数据/（ok）
9个站点的数据/（ok）
工业废气排放监测数据（未拿到）

模型搭建//
1.基本的模型，只用平均值作为预测参照
基线模型


2.线性回归模型 
mlp 但是没有用处 不如基线

3.xgboost 考虑加入一些简单的参数 换data
7.29上午：学习xgboost 下午：代码实践 回来：调优+lstm
gi= 损失函数对前一轮预测值的一阶导数（梯度）。
hi = 损失函数对前一轮预测值的二阶导数（海森矩阵/曲率）
加入了其他站点的数据

4，5. lstm gru(未完成) seq2seq（已完成）
lstm:前置：d2lchapter1-5，8-9 鱼书全部
基于时间序列预测模型的代码。
 


extra.（未来要做的事...）
用别的对比模型（arima ransomforest  simplemlp）
消融实验（两种实验方法的对比）
评估长程预测能力
可解释性，误差分析

